var should = require('should');
var GitController = require('../../../api/controllers/GitController');

describe('controllers:GitController', function(){
  it('Should be tested', function(){
    should.exist(GitController);
  });
});
